// What the document says about itself before any route has a say. Per-route
// titles and descriptions arrive with the SEO slice; until then this is the
// whole of MiCare's metadata, so it states the one thing that distinguishes
// the directory rather than describing a website.

export const SITE_TITLE = 'MiCare — verified opticians near you'

export const SITE_DESCRIPTION =
  'Every optician on MiCare is checked against the General Optical Council register every week. No paid placements: results are ordered by distance.'
